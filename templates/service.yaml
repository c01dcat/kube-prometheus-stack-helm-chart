apiVersion: v1
kind: Service
metadata:
  name: {{ include "kube-prometheus-stack-helm-chart.fullname" . }}
  labels:
    {{- include "kube-prometheus-stack-helm-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kube-prometheus-stack-helm-chart.selectorLabels" . | nindent 4 }}
